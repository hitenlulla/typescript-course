## Compiling single ts to js
> `tsc <ts-file>.ts`

## Watch mode
* Auto compile any changes made to the ts file
> `tsc <ts-file>.ts -w`

## Compile multiple ts files in a project
> `tsc --init`
>
> `tsc -w`

